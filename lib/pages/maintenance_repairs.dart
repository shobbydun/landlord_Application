import 'package:flutter/material.dart';

class MaintenanceRepairs extends StatelessWidget {
  const MaintenanceRepairs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}